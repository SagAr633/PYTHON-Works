# eliminate duplicate elments
list1=[1,4,6,7,9,1,2,3,4,1,2,3]
print(list(set(list1)))

# set does not support duplicate elements,so the best option
# to eleminate duplicate elements from a list is to convert
# it into a set